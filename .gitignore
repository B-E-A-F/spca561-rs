/target
frame_*.ppm
# written by older builds that carried the capture diagnostics
diag.log
trace.log

# RIFE models for mode 4 (mlrt bundle, ~186MB). Fetch separately.
/rife/
rife_v8.7z

# Real-ESRGAN archives from when upscaling existed. Kept ignored so the local
# files do not show up as untracked.
/esrgan/
RealESRGAN*.7z

# Virtual camera build output
/vcam/build/
